[project]
name = "chatterbox-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy>=2.0.0",
    "asyncpg",
    "alembic",
    "cognitojwt[async]",
    "slowapi>=0.1.6"
]
